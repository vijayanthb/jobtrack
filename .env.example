# Either set DATABASE_URL directly...
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jobtrack

# ...or use these individual pieces instead (DATABASE_URL wins if both are set)
PGUSER=postgres
PGPASSWORD=postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=jobtrack
